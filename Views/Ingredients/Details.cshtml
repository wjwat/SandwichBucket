@model SandwichBucket.Models.Ingredient

<h1>@Model.Name</h1>

<h4>Description: @Model.Description</h4>

@if (Model.Sandwiches.Count != 0)
{
  <h2>Sandwiches</h2>
  <ul>
  @foreach (var s in Model.Sandwiches.Select(x => x.Sandwich).ToList())
  {
    <li>@s.Name / @s.Description</li>
  }
  </ul>
}

@if (Model.Tags.Count != 0)
{
  <hr />
  <h2>Tags</h2>
  <ul>
  @foreach (var t in Model.Tags.Select(t => t.Tag).ToList())
  {
    <li>@t.Name</li>
  }
  </ul>
}

<a href="/Ingredients/Edit/@Model.IngredientId"><button type=button>PUT A LITTLE HEAT IN THIS MEAT</button></a>
<a href="/Ingredients/Delete/@Model.IngredientId"><button type=button>I THINK THIS LETTUCE HAS GOT TO GO</button></a>